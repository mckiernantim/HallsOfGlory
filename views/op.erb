 <h3>C://data/operatives/cover==deep/comms_intercepted:<%=User.find(@id).f_name%></h3>
    <% posts = Post.where(user_id: @id).reverse %>
     <h3>C://data/operatives/cover==deep/comms_intercepted:<%=User.find(@id).f_name%></h3>
    <% posts.each do |post|%>
    <div>
       time: <%%>
       title: <%=post.name%>
    </div>
    <div>
        <%=post.body%>
    </div>
    <%end%>